[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
streamlit = "^1.48.1"
pydantic = "^2.11.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
